# Debug Mode

To make debugging the game easier, we'll apply a few special features when the game is launched in
debug mode

## Requirements

1. I want to be able to launch the game in debug mode (`npm run debug`)
2. The debug mode runs the game at 0.25x speed so it's easier to visually identify bugs

